# 项目：小淇（AI 女友智能体，企业微信版）

每次会话开始时：**先读 `摘要.md`**（项目状态、当前卡点、下一步、验证命令），按它继续工作。

工作结束时：如果状态有变化（部署进度、卡点解决、新决策），**更新 `摘要.md`**。

常用命令（详见 摘要.md）：
- `python test_wecom.py` — 企微接入自测（9 项，不联网）
- `python bot.py --dry-run` — 模拟全链路
- `python bot.py` — 正常启动

密钥都在 `config.json`，不要复制进聊天记录或文档。
